#First exercise
temp = [x for x in range(1,16)]

threes_and_fives = filter(lambda x: x % 3 == 0 or x % 5 == 0, temp)

print(threes_and_fives)

#Second exercise
def check_bit4(input):
  mask = 0b1000

  temp = input & mask

  if temp > 0:
    return "on"
  else:
    return "off"


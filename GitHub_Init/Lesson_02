from datetime import datetime
now = datetime.now()

my_string = "Testowy komentarz!"
print(str(len(my_string)))
print(my_string.upper())
print('%02d/%02d/%04d %02d:%02d:%02d' % (now.month, now.day, now.year, now.hour, now.minute, now.second))